{# templates/home/flow.html.twig #}


{% block body %}
 	<!-- #flow steps -->
    <div id="flow" class="bg-blue" >
        <!-- .container -->
    	<div class="container">
            <!-- .row -->
            <div class="row">
                {% if request is defined and request is not empty %}
                    <div style="margin-left: -50px" class="col-md-12 text-center flow">
                        <ul>
                            {% if request.parent is defined and request.parent is not empty %}
                                <li style="margin-top: -15px; margin-right: 15px;">
                                    <a href="{{ path('app_default_slug',{'request': request.parent['@id'] }) }}">
                                        <i class="fas fa-arrow-left fa-3x "></i>
                                        <br/>
                                        <p style="color: white;">Terug</p>
                                    </a>
                                </li>
                            {% endif %}
                            {% for stage in processType.stages %}
                                <li style="margin-top: -15px; margin-right: 15px;" class="{# animation #} {% if (stage.completed  is defined and stage.completed is not empty and stage.completed == true)
                                    or (stage.sufficient is defined and stage.sufficient is not empty and stage.sufficient == true) %}completed{% endif %}" data-animation="animation-bounce-in" data-delay="{{ loop.index*200 }}">
                                    <a href="{{ stage.slug }}">
                                        <i {% if stage.slug == app.request.pathinfo %}  style="border: 4px solid white; background: #2A5587; color: white" {% endif %} class="{{ stage.icon }} fa-3x "></i>
                                        <br/>
                                        {% if stage.name == 'type' %}
                                            <p style="color: white;">CEREMONIE</p>
                                        {% elseif stage.name == 'partners' %}
                                            <p style="color: white;">PARTNER</p>
                                        {% elseif stage.name == 'reserveren' %}
                                            <p style="color: white;">CHECKLIST</p>
                                        {% elseif stage.name == 'extras' %}
                                            <p style="color: white;">EXTRA'S</p>
                                        {% else %}
                                            <p style="color: white;">{{ stage.name|capitalize }}</p>
                                        {% endif %}
                                    </a>
                                </li>
                            {% endfor %}
                        </ul>
                    </div><!-- .col end -->
                {% elseif not app.user %}
                    <div class="col-md-12 text-center">
                            <a href="https://dev.huwelijksplanner.online/digispoof?responceUrl={{ url('app_user_digispoof') }}&backUrl={{ url('app_default_index') }}" class="btn btn-login btn-lg"><span class="fas fa-sign-in-alt"></span> Log hier in om te beginnen</a>
                    </div><!-- .col end -->
                {% else %}
                    <div style="margin-bottom: 20px" class="col-md-12 text-center">
                        <a href="" class="btn btn-login btn-lg"><span class="fas fa-sign-in-alt"></span> Mijn verzoeken</a>
                    </div><!-- .col end -->
                    <div class="col-md-12 text-center">
                        <a href="{{ path('app_wedding_huwelijk') }}" class="btn btn-login btn-lg"><span class="fas fa-sign-in-alt"></span> Verzoek starten</a>
                    </div><!-- .col end -->

                {% endif %}
            </div><!-- .row end -->
        </div><!-- .container end -->
    </div><!-- .bg-blue end -->
{% endblock %}

